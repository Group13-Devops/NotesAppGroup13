<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Note Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="style.css">
  </head>
  <body>
      <%- include('./partials/navbar.ejs') %>
    <div class="singleNote"
    style="width: 800px;
    min-height: 500px;
    margin: auto;
    padding: 20px;
    border: 5px;
    text-align: center; ">
    <h2><%= note.title %></h2> 
    <p class="display-6">Priority: <text style="color: green;"> <%= note.priority %></text></p>
    <hr>
 
    <div style="width: 500px;
                min-height: 100px;
                margin: auto;
                margin-bottom: 15px;
                text-align: left;
                border: 1px;
                border-style: solid;
                padding: 5px;
                padding-left: 20px;
                background-color: rgb(255, 248, 240);">
     <text 
     style="font-size: 18px;
            font-weight: lighter;"><%= note.content %></text>
    </div>
    <% const update = new Date(note.updatedAt); %>
    <% const created = new Date(note.createdAt); %>

    <p><text  >Created on: <%= update.toLocaleString() %>
       |   Last Updated on: <%= update.toLocaleString() %></text></p>

       <a href="/notes/<%= note._id %>/edit"><button class="btn btn-outline-warning" style="width: 100px;">Edit</button></a>
                <form action="/notes/<%= note._id %>" method="POST"
                    onsubmit="return confirm('Are you sure you want to delete this note?');">
                    <input type="hidden" name="_method" value="DELETE">
                    <button class="btn btn-outline-danger" style="width: 100px;" type="submit">Delete</button>
                </form>

    </div>
</body>
</html>
